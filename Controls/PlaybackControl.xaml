<UserControl x:Class="KeyBard.Controls.PlaybackControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="1200">
    <UserControl.Resources>
        <Style x:Key="PlaybackButton" TargetType="Button">
            <Setter Property="MinWidth" Value="90" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="6,0" />
            <Setter Property="Padding" Value="14,4" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.85" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.65" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="#FF1E1E1E" Padding="12,8" BorderBrush="#FF333333" BorderThickness="0,0,0,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <!-- Playback Group -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="BtnPlay" Content="▶  Play"
                        Background="#FF2E8B57"
                        Style="{StaticResource PlaybackButton}"
                        Click="BtnPlay_Click" IsEnabled="False" Margin="0,0,8,0" />
                <Button x:Name="NameBtnPause" x:FieldModifier="private" Content="⏸  Pause"
                        Background="#FFD4A017"
                        Style="{StaticResource PlaybackButton}"
                        Click="BtnPause_Click" IsEnabled="False" Margin="0,0,8,0" />
                <Button x:Name="BtnStop" Content="⏹  Stop"
                        Background="#FFC03030"
                        Style="{StaticResource PlaybackButton}"
                        Click="BtnStop_Click" IsEnabled="False" Margin="0,0,8,0" />
                <Button x:Name="BtnRestart" Content="⏮  Restart"
                        Background="#FF4682B4"
                        Style="{StaticResource PlaybackButton}" MinWidth="110"
                        Click="BtnRestart_Click" IsEnabled="False" Margin="0,0,0,0" />
            </StackPanel>

            <!-- A-B Loop Group -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="A-B Loop:" Foreground="#FFBBBBBB" VerticalAlignment="Center" Margin="0,0,12,0"
                           FontWeight="SemiBold" />
                <Button x:Name="BtnSetLoopStart" Content="Set A" Width="90" Height="28" Click="BtnSetLoopStart_Click"
                        Style="{StaticResource PlaybackButton}" Background="#FF4A90E2" Margin="0,0,6,0" />
                <Button x:Name="BtnSetLoopEnd" Content="Set B" Width="90" Height="28" Click="BtnSetLoopEnd_Click"
                        Style="{StaticResource PlaybackButton}" Background="#FF4A90E2" Margin="0,0,6,0" />
                <Button x:Name="BtnClearLoop" Content="Clear" Width="90" Height="28" Click="BtnClearLoop_Click"
                        Style="{StaticResource PlaybackButton}" Background="#FF666666" Margin="0,0,0,0" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>