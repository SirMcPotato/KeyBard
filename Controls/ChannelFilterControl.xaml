<UserControl x:Class="KeyBard.Controls.ChannelFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="50" d:DesignWidth="1200">
    <Border Background="#FF252525" Padding="10,4" BorderBrush="#FF333333" BorderThickness="0,1">
        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="MIDI Channels:" Foreground="#FFBBBBBB" VerticalAlignment="Center" Margin="0,4,10,4"
                       FontWeight="SemiBold" />
            <ItemsControl x:Name="ChannelsItemsControl">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsActive}"
                                  IsEnabled="{Binding ExistsInFile}"
                                  Foreground="White"
                                  Margin="4,0"
                                  VerticalAlignment="Center" d:DataContext="{d:DesignInstance }">
                            <CheckBox.Style>
                                <Style TargetType="CheckBox">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="CheckBox">
                                                <Border x:Name="Box" Background="#FF333333" CornerRadius="3"
                                                        Padding="6,2" Margin="2"
                                                        BorderBrush="{Binding HexColor}" BorderThickness="0,0,0,3">
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="{Binding ChannelNumber}" FontWeight="Bold"
                                                                   Margin="0,0,4,0" />
                                                        <TextBlock Text="{Binding InstrumentName}" FontSize="10"
                                                                   VerticalAlignment="Bottom" Opacity="0.8" />
                                                    </StackPanel>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="Box" Property="Background"
                                                                Value="#FF4A90E2" />
                                                    </Trigger>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter TargetName="Box" Property="Opacity" Value="0.8" />
                                                    </Trigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter TargetName="Box" Property="Opacity" Value="0.2" />
                                                        <Setter Property="Foreground" Value="Gray" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </CheckBox.Style>
                        </CheckBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </WrapPanel>
    </Border>
</UserControl>